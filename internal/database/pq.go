package database

import (
	"github.com/jmoiron/sqlx"
	"log"
)

func Open(log *log.Logger) (*sqlx.DB, error) {

	/*	connStr := "user=pqgotest dbname=pqgotest sslmode=verify-full"

		//db, err := sqlx.Open("postgres", connStr)

		if err != nil {
			log.Fatal(err)
		}*/

	return nil, nil

}
